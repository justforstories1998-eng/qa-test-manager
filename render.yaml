# Root render.yaml - Backend deployment only
# Frontend is deployed to Netlify (see frontend/netlify.toml)
#
# To deploy backend:
# 1. Go to render.com → New + → Web Service
# 2. Connect your GitHub repo
# 3. Set Root Directory to "backend"
# 4. Or use Blueprint and this render.yaml will auto-configure
